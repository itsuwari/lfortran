#include <inttypes.h>

#include <stdlib.h>
#include <stdbool.h>
#include <stdio.h>
#include <string.h>
#include <lfortran_intrinsics.h>


#ifndef LFORTRAN_C_BACKEND_CONSTRUCTOR
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_CONSTRUCTOR __attribute__((constructor))
#else
#define LFORTRAN_C_BACKEND_CONSTRUCTOR
#endif
#endif
#ifndef LFORTRAN_C_BACKEND_WEAK
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_WEAK __attribute__((weak))
#else
#define LFORTRAN_C_BACKEND_WEAK static
#endif
#endif
#ifndef LFORTRAN_C_BACKEND_COMMON
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_COMMON __attribute__((common))
#else
#define LFORTRAN_C_BACKEND_COMMON
#endif
#endif




// Implementations
int main(int argc, char* argv[])
{
    _lpython_set_argv(argc, argv);
    int32_t count;
    int32_t ix;
    int32_t jx;
    int32_t total;
    ix = 1;
    count = 0;
    total = 0;
    int32_t loop_end___0 = (ix > 0 ? -1 : 1);
    for (jx = 1; (((ix > 0 ? -2 : 2) >= 0) && (jx <= loop_end___0)) || (((ix > 0 ? -2 : 2) < 0) && (jx >= loop_end___0)); jx += (ix > 0 ? -2 : 2)) {
        count = count + 1;
        total = total + jx;
    }
    if (count != 2 || total != 0) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    ix = -1;
    count = 0;
    total = 0;
    int32_t loop_end___1 = (ix > 0 ? -1 : 3);
    for (jx = 1; (((ix > 0 ? -2 : 1) >= 0) && (jx <= loop_end___1)) || (((ix > 0 ? -2 : 1) < 0) && (jx >= loop_end___1)); jx += (ix > 0 ? -2 : 1)) {
        count = count + 1;
        total = total + jx;
    }
    if (count != 3 || total != 6) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    return 0;
}
