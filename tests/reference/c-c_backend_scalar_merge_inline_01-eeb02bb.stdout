#include <inttypes.h>
#include <math.h>

#include <stdlib.h>
#include <stdbool.h>
#include <stdio.h>
#include <string.h>
#include <lfortran_intrinsics.h>


#ifndef LFORTRAN_C_BACKEND_CONSTRUCTOR
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_CONSTRUCTOR __attribute__((constructor))
#else
#define LFORTRAN_C_BACKEND_CONSTRUCTOR
#endif
#endif
#ifndef LFORTRAN_C_BACKEND_WEAK
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_WEAK __attribute__((weak))
#else
#define LFORTRAN_C_BACKEND_WEAK static
#endif
#endif
#ifndef LFORTRAN_C_BACKEND_COMMON
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_COMMON __attribute__((common))
#else
#define LFORTRAN_C_BACKEND_COMMON
#endif
#endif




// Implementations
int main(int argc, char* argv[])
{
    _lpython_set_argv(argc, argv);
    int32_t a;
    int32_t b;
    bool m;
    double x;
    a = 7;
    b = 3;
    m = true;
    int32_t __lfortran_merge_tsource = a;
    int32_t __lfortran_merge_fsource = b;
    bool __lfortran_merge_mask = m;
    if ((__lfortran_merge_mask ? __lfortran_merge_tsource : __lfortran_merge_fsource) != 7) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    m = false;
    int32_t __lfortran_merge_tsource1 = a;
    int32_t __lfortran_merge_fsource1 = b;
    bool __lfortran_merge_mask1 = m;
    if ((__lfortran_merge_mask1 ? __lfortran_merge_tsource1 : __lfortran_merge_fsource1) != 3) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    double __lfortran_merge_tsource2 = 0x1.4000000000000p+1;
    double __lfortran_merge_fsource2 = -0x1.8000000000000p+0;
    bool __lfortran_merge_mask2 = a > b;
    x = (__lfortran_merge_mask2 ? __lfortran_merge_tsource2 : __lfortran_merge_fsource2);
    if (fabs(x - 0x1.4000000000000p+1) > 0x1.19799812dea11p-40) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    return 0;
}
