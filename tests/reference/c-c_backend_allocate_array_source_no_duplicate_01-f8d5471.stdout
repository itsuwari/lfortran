#include <inttypes.h>
#include <string.h>

#include <stdlib.h>
#include <stdbool.h>
#include <stdio.h>
#include <string.h>
#include <lfortran_intrinsics.h>


#ifndef LFORTRAN_C_BACKEND_CONSTRUCTOR
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_CONSTRUCTOR __attribute__((constructor))
#else
#define LFORTRAN_C_BACKEND_CONSTRUCTOR
#endif
#endif
#ifndef LFORTRAN_C_BACKEND_WEAK
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_WEAK __attribute__((weak))
#else
#define LFORTRAN_C_BACKEND_WEAK static
#endif
#endif
#ifndef LFORTRAN_C_BACKEND_COMMON
#if defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_COMMON __attribute__((common))
#else
#define LFORTRAN_C_BACKEND_COMMON
#endif
#endif

#ifndef LFORTRAN_C_BACKEND_THREAD_LOCAL
#if defined(_MSC_VER)
#define LFORTRAN_C_BACKEND_THREAD_LOCAL __declspec(thread)
#elif defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_THREAD_LOCAL __thread
#elif defined(__STDC_VERSION__) && __STDC_VERSION__ >= 201112L
#define LFORTRAN_C_BACKEND_THREAD_LOCAL _Thread_local
#else
#define LFORTRAN_C_BACKEND_THREAD_LOCAL
#endif
#endif

#ifndef LFORTRAN_C_BACKEND_TBP_CACHE_STORAGE
#if defined(_MSC_VER)
#define LFORTRAN_C_BACKEND_TBP_CACHE_STORAGE static __declspec(thread)
#elif defined(__GNUC__) || defined(__clang__)
#define LFORTRAN_C_BACKEND_TBP_CACHE_STORAGE static __thread
#elif defined(__STDC_VERSION__) && __STDC_VERSION__ >= 201112L
#define LFORTRAN_C_BACKEND_TBP_CACHE_STORAGE static _Thread_local
#else
#define LFORTRAN_C_BACKEND_TBP_CACHE_STORAGE
#endif
#endif


struct dimension_descriptor
{
    int32_t lower_bound, length, stride;
};

struct r64_____
{
    double *data;
    struct dimension_descriptor dims[32];
    int32_t n_dims;
    int32_t offset;
    bool is_allocated;
};




// Implementations
int main(int argc, char* argv[])
{
    _lpython_set_argv(argc, argv);
    lfortran_allocator_t* __lfortran_default_allocator = _lfortran_get_default_allocator();
    int32_t __libasr_index_0_;
    int32_t __libasr_index_0_1;
    int32_t __libasr_index_1_;
    int32_t __libasr_index_1_1;
    struct r64_____ dst_value;
    struct r64_____* dst = &dst_value;
    double *dst_data = NULL;
    dst->data = dst_data; dst->n_dims = 2; dst->offset = 0; dst->is_allocated = false;
    dst->dims[0].lower_bound = 1; dst->dims[0].length = 0; dst->dims[0].stride = 1;
    dst->dims[1].lower_bound = 1; dst->dims[1].length = 0; dst->dims[1].stride = (1*0);
    int32_t i;
    int32_t j;
    struct r64_____ src_value;
    struct r64_____* src = &src_value;
    double *src_data = NULL;
    src->data = src_data; src->n_dims = 2; src->offset = 0; src->is_allocated = false;
    src->dims[0].lower_bound = 1; src->dims[0].length = 0; src->dims[0].stride = 1;
    src->dims[1].lower_bound = 1; src->dims[1].length = 0; src->dims[1].stride = (1*0);
    if (src == NULL) {
        src = _lfortran_malloc_alloc(__lfortran_default_allocator, sizeof(*src));
        memset(src, 0, sizeof(*src));
    }
    src->n_dims = 2;
    src->dims[0].lower_bound = 1;
    src->dims[0].length = 2;
    src->dims[0].stride = 1;
    src->dims[1].lower_bound = 1;
    src->dims[1].length = 3;
    src->dims[1].stride = (1 * 2);
    src->offset = 0;
    src->data = (double*) _lfortran_malloc_alloc(__lfortran_default_allocator, 1*src->dims[0].length*src->dims[1].length*sizeof(double));
    src->is_allocated = true;
    for (j=1; j<=3; j++) {
        for (i=1; i<=2; i++) {
            src->data[(((0 + (src->dims[0].stride * (i - src->dims[0].lower_bound))) + (src->dims[1].stride * (j - src->dims[1].lower_bound))) + src->offset)] = 0x1.4000000000000p+3*(double)(i) + (double)(j);
        }
    }
    if (dst == NULL) {
        dst = _lfortran_malloc_alloc(__lfortran_default_allocator, sizeof(*dst));
        memset(dst, 0, sizeof(*dst));
    }
    dst->n_dims = 2;
    dst->dims[0].lower_bound = ((int32_t)src->dims[1-1].lower_bound);
    dst->dims[0].length = ((int32_t)src->dims[1-1].length);
    dst->dims[0].stride = 1;
    dst->dims[1].lower_bound = ((int32_t)src->dims[2-1].lower_bound);
    dst->dims[1].length = ((int32_t)src->dims[2-1].length);
    dst->dims[1].stride = (1 * ((int32_t)src->dims[1-1].length));
    dst->offset = 0;
    dst->data = (double*) _lfortran_malloc_alloc(__lfortran_default_allocator, 1*dst->dims[0].length*dst->dims[1].length*sizeof(double));
    dst->is_allocated = true;
    __libasr_index_1_1 = ((int32_t)src->dims[2-1].lower_bound);
    for (__libasr_index_1_=((int32_t)dst->dims[2-1].lower_bound); __libasr_index_1_<=((int32_t) dst->dims[2-1].length + dst->dims[2-1].lower_bound - 1); __libasr_index_1_++) {
        __libasr_index_0_1 = ((int32_t)src->dims[1-1].lower_bound);
        for (__libasr_index_0_=((int32_t)dst->dims[1-1].lower_bound); __libasr_index_0_<=((int32_t) dst->dims[1-1].length + dst->dims[1-1].lower_bound - 1); __libasr_index_0_++) {
            dst->data[(((0 + (dst->dims[0].stride * (__libasr_index_0_ - dst->dims[0].lower_bound))) + (dst->dims[1].stride * (__libasr_index_1_ - dst->dims[1].lower_bound))) + dst->offset)] = src->data[(((0 + (src->dims[0].stride * (__libasr_index_0_1 - src->dims[0].lower_bound))) + (src->dims[1].stride * (__libasr_index_1_1 - src->dims[1].lower_bound))) + src->offset)];
            __libasr_index_0_1 = __libasr_index_0_1 + 1;
        }
        __libasr_index_1_1 = __libasr_index_1_1 + 1;
    }
    if (((int32_t)dst->dims[1-1].length) != 2) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    if (((int32_t)dst->dims[2-1].length) != 3) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    if (dst->data[(((0 + (dst->dims[0].stride * (1 - dst->dims[0].lower_bound))) + (dst->dims[1].stride * (1 - dst->dims[1].lower_bound))) + dst->offset)] != src->data[(((0 + (src->dims[0].stride * (1 - src->dims[0].lower_bound))) + (src->dims[1].stride * (1 - src->dims[1].lower_bound))) + src->offset)]) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    if (dst->data[(((0 + (dst->dims[0].stride * (2 - dst->dims[0].lower_bound))) + (dst->dims[1].stride * (3 - dst->dims[1].lower_bound))) + dst->offset)] != src->data[(((0 + (src->dims[0].stride * (2 - src->dims[0].lower_bound))) + (src->dims[1].stride * (3 - src->dims[1].lower_bound))) + src->offset)]) {
        fprintf(stderr, "ERROR STOP");
        exit(1);
    }
    if ((src) != NULL && (src)->is_allocated && (src)->data != NULL) {
        _lfortran_free_alloc_plain(__lfortran_default_allocator, (char*) (src)->data);
        (src)->data = NULL;
    }
    if ((src) != NULL) {
        (src)->offset = 0;
        (src)->is_allocated = false;
    }
    if ((dst) != NULL && (dst)->is_allocated && (dst)->data != NULL) {
        _lfortran_free_alloc_plain(__lfortran_default_allocator, (char*) (dst)->data);
        (dst)->data = NULL;
    }
    if ((dst) != NULL) {
        (dst)->offset = 0;
        (dst)->is_allocated = false;
    }
    return 0;
}
